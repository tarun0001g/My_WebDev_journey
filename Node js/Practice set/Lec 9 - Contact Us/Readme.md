
First we will intall default package.json
command :- npm init -y 

now we will create app.js file 

then we will add scrpit in package.json to start the server using nodemon

Then install nodemon:- npm install --save-dev nodemon

Then we will install express.js :- npm install express --save

then add basic code in the app.js file 

then start actual coding of middlewares and Routing 

