

function Chai(){
    return(
        <h2>Chai, Please!</h2>
    )
}

export default Chai


// Understandings
// we created one component here named as chai.js
// we returned one function that is also one component
// Rules for fn component : Always use first capital letter for the name of Function
// also exclude index.js, every component should have first letter capital like App.js